spring:
  application:
    name: code-club-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 36.139.122.101:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: 36.139.122.101:8848